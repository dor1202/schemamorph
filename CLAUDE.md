@AGENTS.md

Use TodoWrite for any multi-step task (3+ steps) — track each step explicitly.

Run the full gate before claiming a task done:
`npm run typecheck && npm run lint && npm run format:check && npm run validate:catalog && npm run test && npm run build`
